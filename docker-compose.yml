version: '3.8'

services:
  reviewbisnisku:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: reviewbisnisku-app
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - .env
